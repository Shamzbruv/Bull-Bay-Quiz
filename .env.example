# Optional. Leave unset to run fully offline on localStorage (the default).
# Set both to switch persistence over to Supabase — see supabase/migrations
# for the schema to apply first, and src/lib/storage for the adapter.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
